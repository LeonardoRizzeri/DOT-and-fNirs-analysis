# External Dependencies

This directory is reserved for third-party MATLAB toolboxes used locally by the analysis.

Expected dependency:

- `iso2mesh-master/`, used for mesh handling and visualization through functions such as `plotmesh`.

Do not commit full third-party toolboxes to this repository. Install or copy them locally into this directory, or add them to the MATLAB path from their own location.
